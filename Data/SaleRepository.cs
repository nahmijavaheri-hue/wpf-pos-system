namespace PointOfSale.Data;

// Saves completed sales and reads them back for the history screen.
// TODO: record a sale + its lines (and drop the stock), get recent sales, today's totals
public class SaleRepository
{
}
