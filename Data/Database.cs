namespace PointOfSale.Data;

// Sets up the SQLite database. Creates the tables on first run and gives the
// rest of the app a connection to work with.
// TODO: connection string, create tables (products, sales, sale lines), seed some demo items
public static class Database
{
}
