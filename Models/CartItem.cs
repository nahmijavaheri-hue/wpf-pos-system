namespace PointOfSale.Models;

// A single line in the current sale - a product plus how many of it.
// Needs to tell the UI when the quantity changes so the totals update live.
// TODO: hold a Product + a quantity, and work out the line total
public class CartItem
{
}
